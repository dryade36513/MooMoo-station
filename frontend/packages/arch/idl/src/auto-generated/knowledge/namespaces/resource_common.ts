/*
 * Copyright 2025 coze-dev Authors
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
/* eslint-disable */
/* tslint:disable */
// @ts-nocheck

export type Int64 = string | number;

export enum ActionKey {
  /** 複製 */
  Copy = 1,
  /** 刪除 */
  Delete = 2,
  /** 啓用/禁用 */
  EnableSwitch = 3,
  /** 編輯 */
  Edit = 4,
  /** 跨空間複製 */
  CrossSpaceCopy = 10,
}

export enum CopyStatus {
  Successful = 1,
  Processing = 2,
  Failed = 3,
  /** 如果是KeepOrigin，表示該資源不需要做變更，資源方不需要設置Target相關信息；引用方直接忽略即可 */
  KeepOrigin = 4,
}

export enum ProjectResourceActionKey {
  /** 重命名 */
  Rename = 1,
  /** 創建副本/複製到當前項目 */
  Copy = 2,
  /** 複製到資源庫 */
  CopyToLibrary = 3,
  /** 移動到資源庫 */
  MoveToLibrary = 4,
  /** 刪除 */
  Delete = 5,
  /** 啓用 */
  Enable = 6,
  /** 禁用 */
  Disable = 7,
  /** 切換成funcflow */
  SwitchToFuncflow = 8,
  /** 切換成chatflow */
  SwitchToChatflow = 9,
}

export enum ProjectResourceGroupType {
  Workflow = 1,
  Plugin = 2,
  Data = 3,
}

export enum PublishStatus {
  /** 未發佈 */
  UnPublished = 1,
  /** 已發佈 */
  Published = 2,
}

export enum ResourceCopyScene {
  /** 複製項目內的資源，淺拷貝 */
  CopyProjectResource = 1,
  /** 複製項目資源到Library，複製後要發佈 */
  CopyResourceToLibrary = 2,
  /** 移動項目資源到Library，複製後要發佈，後置要刪除項目資源 */
  MoveResourceToLibrary = 3,
  /** 複製Library資源到項目 */
  CopyResourceFromLibrary = 4,
  /** 複製項目 */
  CopyProject = 5,
  /** 項目發佈到渠道，連帶資源需要發佈（含商店）。以當前草稿發佈。 */
  PublishProject = 6,
  /** 複製項目模板。 */
  CopyProjectTemplate = 7,
  /** 項目發佈到模板，以項目的指定版本發佈成模板。 */
  PublishProjectTemplate = 8,
  /** 上架模板 */
  LaunchTemplate = 9,
  /** 單個資源跨空間複製 */
  CrossSpaceCopy = 12,
  /** 項目跨空間複製 */
  CrossSpaceCopyProject = 13,
}

export enum ResType {
  Plugin = 1,
  Workflow = 2,
  Imageflow = 3,
  Knowledge = 4,
  UI = 5,
  Database = 7,
  Variable = 8,
}

export enum SyncOperation {
  Upsert = 1,
  Delete = 2,
}

/** 展示用，實現方提供展示信息 */
export interface DisplayResourceInfo {
  /** 資源id */
  ResID?: Int64;
  /** 資源描述 */
  Desc?: string;
  /** 資源Icon，完整url */
  Icon?: string;
  /** 資源狀態，各類型資源自身定義 */
  BizResStatus?: number;
  /** 是否開啓多人編輯 */
  CollaborationEnable?: boolean;
  /** 業務攜帶的擴展信息，以res_type區分，每個res_type定義的schema和含義不一樣，使用前需要判斷res_type */
  BizExtend?: Record<string, string>;
  /** 不同類型的不同操作按鈕，由資源實現方和前端約定。返回則展示，要隱藏某個按鈕，則不要返回； */
  Actions?: Array<ResourceAction>;
  /** 是否禁止進詳情頁 */
  DetailDisable?: boolean;
  /** 資源名稱 */
  Name?: string;
  /** 資源發佈狀態，1-未發佈，2-已發佈 */
  PublishStatus?: PublishStatus;
  /** 最近編輯時間, unix秒級時間戳 */
  EditTime?: Int64;
}

export interface ProjectResourceAction {
  /** 一個操作對應一個唯一的key，key由資源側約束 */
  key: ProjectResourceActionKey;
  /** ture=可以操作該Action，false=置灰 */
  enable: boolean;
  /** enable=false時，提示文案。後端返回Starling Key，注意放在同一個space下。 */
  hint?: string;
}

/** 實現方提供展示信息 */
export interface ProjectResourceInfo {
  /** 資源id */
  res_id?: Int64;
  /** 資源名稱 */
  name?: string;
  /** 不同類型的不同操作按鈕，由資源實現方和前端約定。返回則展示，要隱藏某個按鈕，則不要返回； */
  actions?: Array<ProjectResourceAction>;
  /** 該用戶是否對資源只讀
4: bool ReadOnly (go.tag = "json:\"read_only\"", agw.key = "read_only")
資源類型 */
  res_type?: ResType;
  /** 資源子類型，由資源實現方定義。Plugin：1-Http; 2-App; 6-Local；Knowledge：0-text; 1-table; 2-image；UI：1-Card */
  res_sub_type?: number;
  /** 業務攜帶的擴展信息，以res_type區分，每個res_type定義的schema和含義不一樣，使用前需要判斷res_type */
  biz_extend?: Record<string, string>;
  /** 資源狀態，各類型資源自身定義。前端與各資源方約定。 */
  biz_res_status?: number;
  res_third_type?: number;
}

export interface RefTreeNode {
  ResID?: Int64;
  ResType?: ResType;
  /** 引用的子資源，子資源在不同層級的引用可以出現多次。A-B, A-C-B, A-D, B可以同時在A和C的ChildrenNodes出現 */
  ChildrenNodes?: Array<RefTreeNode>;
}

/** Library資源操作 */
export interface ResourceAction {
  /** 一個操作對應一個唯一的key，key由資源側約束 */
  key: ActionKey;
  /** ture=可以操作該Action，false=置灰 */
  enable: boolean;
}

export interface ResourceCopyCheckFailedReason {
  ResourceLocator?: ResourceLocator;
  ResName?: string;
  Reason?: string;
}

export interface ResourceCopyEnv {
  Scene?: ResourceCopyScene;
  /** 原項目ID。如果被複制的資源在項目中，則有值。場景：CopyProjectResource、CopyResourceToLibrary、MoveResourceToLibrary、CopyProject */
  OriginProjectID?: Int64;
  /** 目標項目ID。如果複製後的資源要賦值project_id，則有值。場景：CopyProjectResource、CopyResourceFromLibrary、CopyProject */
  TargetProjectID?: Int64;
  /** 被用戶選擇複製/移動的資源ID。如果操作的目標是資源，則有值。場景：CopyProjectResource、CopyResourceToLibrary、MoveResourceToLibrary、CopyResourceFromLibrary */
  ResourceLocator?: ResourceLocator;
  /** 當次任務的唯一約束，控制冪等。有則傳，發起任務時不用傳 */
  TaskUniqKey?: string;
  /** 項目發佈時，項目版本。場景：PublishProject、PublishProjectTemplate */
  TargetProjectVersion?: Int64;
  OriginProjectSpaceID?: Int64;
  TargetProjectSpaceID?: Int64;
  /** 操作者用戶id */
  CurrentUserID?: Int64;
  /** 發佈模板時，原項目版本。or 複製模板時，模板的項目版本。 */
  OriginProjectVersion?: Int64;
  /** 0/default-app，默認是app也就是之前的project; 1-bot或者叫agent */
  ProjectType?: number;
}

/** 每個資源的複製結果，包含前後映射信息 */
export interface ResourceCopyResult {
  OriginResourceLocator?: ResourceLocator;
  TargetResourceLocator?: ResourceLocator;
  CopyStatus?: CopyStatus;
  /** 發佈版本號或版本名，0.0.1 */
  TargetPublishVersionNum?: string;
  /** 其他的信息，比如plugin的tool映射信息。schema和各資源方約定 */
  TargetResInfo?: string;
}

/** 用於定位一個資源數據的結構：某資源的某版本 */
export interface ResourceLocator {
  ResID?: Int64;
  ResType?: ResType;
  /** 資源的當前版本，沒有則是最新版本。項目發佈版本或Library發佈版本 */
  PublishVersion?: Int64;
}
/* eslint-enable */
